import React from 'react'

const PageTitle = ({title}) => {
    return (
        <div>
            <h1 className='text-4xl text-center font-bold text-gray-700 my-6'>{title}</h1>
        </div>
    )
}

export default PageTitle
