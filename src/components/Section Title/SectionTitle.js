import React from 'react'

const SectionTitle = ({ title }) => {
    return <h1 className="text-primary text-left py-3 uppercase text-2xl font-bold border-b">{title}</h1>

}

export default SectionTitle
