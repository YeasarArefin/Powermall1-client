import React from 'react';

const SectionTitle = ({ title }) => {
    return <h1 className="text-primary text-left py-3 capitalize text-2xl font-bold border-b border-gray-200">{title}</h1>;

};

export default SectionTitle;
